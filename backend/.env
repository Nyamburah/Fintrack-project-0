# MongoDB Connection
MONGO_URI=mongodb+srv://Nyamzy:nyambura1261@fintrack.bem3xe2.mongodb.net/?retryWrites=true&w=majority&appName=Fintrack

# JWT Secret for token generation (IMPORTANT: Change this in production!)
JWT_SECRET=fintrack_super_secure_jwt_secret_key_2024_change_in_production

MPESA_ENVIRONMENT=sandbox

# Server Configuration
PORT=8000
NODE_ENV=development

# React App API URL
REACT_APP_API_URL=http://localhost:8000/api

# Ngrok (update with your actual ngrok URL when needed)
NGROK_URL=https://your-new-ngrok-url.ngrok-free.app

# Optional: Add these for additional security
BCRYPT_ROUNDS=12
TOKEN_EXPIRY=7d